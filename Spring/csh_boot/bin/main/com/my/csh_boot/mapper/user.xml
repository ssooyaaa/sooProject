<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">	
	 		

	<select id="findByIdx" parameterType="User" resultType="User">
		SELECT * FROM user
		WHERE user_idx=#{user_idx}
	</select>
	
</mapper>